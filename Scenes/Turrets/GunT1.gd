extends "res://Scenes/Turrets/turrets.gd"


#func _on_mouse_entered() -> void:
	#print_rich("[font_size=15][color=cornsilk]GunT1 _on_mouse_entered Mouse entered GunT1 collision shape")
	#print("position = ", position)
	### goal is to trigger an upgrade popup
	### if left click and maybe some other checks to make sure there is no conflict
	### then trigger upgrade popup.  read about input_pickable.  do I need a collisionbody2d?
#
#func _on_mouse_exited() -> void:
	#pass # Replace with function body.


#func _on_input_event(viewport: Node, event: InputEvent, shape_idx: int) -> void:
	#pass # Replace with function body.
